<%= form_for(@part_type) do |f| %>
  <% if @part_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@part_type.errors.count, "error") %> prohibited this part_type from being saved:</h2>

      <ul>
      <% @part_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <% if @part_type.id.nil? %>
      <h4>Ідентифікатор нового типу для частин буде - <%= PartType.last.nil? ? 1 : PartType.last.id + 1 %></h4>
    <% else %>
      <h4>Редагуємо тип частини з ідентифікатором - <%= @part_type.id %></h4>
    <% end %>
  </div>

  <div class="field">
    <%= f.label "Тип частини" %><br>
    <%= f.text_field :type_name %>
  </div>
  <div class="actions">
    <%= f.submit "Зберегти"%>
  </div>
<% end %>
